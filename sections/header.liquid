<!DOCTYPE html>
  <html lang="es">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
      /* Reset y Estilos Generales */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  font-family: Arial, sans-serif;
}

/* Contenedor General */
.container {
  width: 100%;
  margin: 0 auto;
}

/* Layout General */
.layout {
  display: flex;
  flex-wrap: wrap;
  width: 100%;
  justify-content: space-between;
}

.column {
  padding: 20px;
}

.half {
  width: 50%;
}

.third {
  width: 33.33%;
}

.full {
  width: 100%;
}

/* Hero Section */
.hero {
  color: white;
  height: 785px;
  position: relative;
  background-image: url('assets/group@2x.png');
  background-size: cover;
  background-position: center;
}

.logo-tennis {
  width: 37px;
  height: 37px;
  margin-top: 25px;
  margin-left: 36px;
}

.hero-content {
  position: absolute;
  top: 50%;
  left: 173px;
  transform: translateY(-50%);
  text-align: left;
}

.hero-content h1 {
  font-size: 53px;
  font-weight: normal;
  color: white;
  max-width: 600px;
  word-wrap: break-word;
  line-height: 1.2;
}

.strong-text {
  color: #cbe346;
}

.hero-button {
  padding: 10px 20px;
  border: 1px solid white; /* Define un borde visible */
  background-color: transparent; /* Hace el fondo transparente */
  color: white; /* El color del texto */
  font-size: 1rem;
  cursor: pointer;
  border-radius: 26.1px;
  margin-top: 25px;
  font-size: 13px;
  font-weight: bold;
  font-stretch: normal;
  font-style: normal;
  line-height: 2.15;
  letter-spacing: 1.01px;
  background-image: url('{{ "path.svg" | asset_url }}'); /* Shopify: ruta de la imagen */
  background-repeat: no-repeat;
  background-position: right 10px center; /* Coloca la flecha a la derecha */
  background-size: 16px 16px; /* Tamaño de la imagen */
  padding-right: 40px; /* Espacio para la flecha */
}

.container.raquetas-section {
  background-color: #f3f4f8;
  height: 392px;
}

.column.third.text-left {
  padding: 72px 0 0 255px;
}

.column.third.text-center {
  padding: 72px 0 0 51px;
}

.column.third.text-right {
  padding: 272px 0 67px 168px;
}

.title-center {
  color: #1e2335;
  font-size: 50px;
  font-weight: normal;
  font-stretch: normal;
  font-style: normal;
  line-height: 1.14;
  letter-spacing: -0.06px;
}

.title-left {
  font-size: 15px;
  font-weight: 500;
  color: #cbe346;
  line-height: 2;
  letter-spacing: 1.87px;
  margin-left: 77px;
}

p.paragraph-about {
  width: 393px;
  height: 87px;
  margin: 15px 0;
  font-size: 18px;
  font-weight: normal;
  font-stretch: normal;
  font-style: normal;
  line-height: 1.78;
  letter-spacing: 0.51px;
  color: #787878;
}

/* Contenedor principal-seccion3 */
/* Fondo blanco principal */
.products {
  background-color: white;
  display: flex;
  flex-wrap: wrap;
}

/* Imagen con texto superpuesto */
.image-column {
  width: 530px;
  height: 468px;
  position: relative;
  background-image: url('{{ "ball-color-court-1405355.png" | asset_url }}'); /* Shopify: ruta de la imagen */
  background-size: cover;
  background-position: center;
}

.text-overlay {
  position: absolute;
  top: 30%;
  left: 10%;
  transform: translateY(-30%);
  color: white;
}

.text-overlay h2 {
  font-size: 32px;
  margin: 0 0 10px 0;
}

.text-overlay strong {
  color: #d4e157;
}

.text-overlay p {
  font-size: 16px;
  margin: 0 0 20px 0;
}

/* Controles del carrusel */
.carousel-controls {
  display: flex;
  align-items: center;
  gap: 20px;
}

.carousel-indicators {
  display: flex;
  gap: 8px;
}

.dot {
  width: 10px;
  height: 10px;
  background-color: #ccc;
  border-radius: 50%;
  cursor: pointer;
}

.dot.active {
  background-color: #d4e157;
}

.arrows {
  display: flex;
  gap: 10px;
}

.arrow-left, .arrow-right {
  background-color: rgba(255, 255, 255, 0.5);
  border: none;
  border-radius: 50%;
  width: 30px;
  height: 30px;
  color: #333;
  font-size: 16px;
  cursor: pointer;
}

/* Carrusel de productos */
.carousel-column {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  overflow: hidden;
  background-color: white;
}

.carousel {
  width: 100%;
  overflow-x: auto;
  scroll-behavior: smooth;
}

.carousel-inner {
  display: flex;
  gap: 20px;
}



.img-carousel {
  width: 288px;
  height: 344px;
}

/* Círculo numerado */
.circle {
  position: absolute;
  top: 10px;
  left: 10px;
  width: 25px;
  height: 25px;
  background-color: #d4e157;
  color: white;
  font-size: 14px;
  font-weight: bold;
  text-align: center;
  line-height: 25px;
  border-radius: 50%;
}





/* Footer Section */
.footer {
  background-color: #d4e157;
  color: #000;
  padding: 40px;
}

.footer-logo {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.footer-menu {
  display: flex;
  list-style-type: none;
}

.footer-menu li {
  margin-right: 20px;
  cursor: pointer;
}

.footer-menu a {
  color: black;
  text-decoration: none;
}

.footer-menu a:hover {
  color: #c0d836;
}

.footer-social-icons {
  display: flex;
  justify-content: space-between;
}

.footer-social-icons a {
  color: black;
  margin-right: 15px;
  font-size: 1.2rem;
}

.footer-social-icons a:hover {
  color: #c0d836;
}

/* Estilos Temporales */
header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 10px 20px;
  position: absolute;
  width: 100%;
  z-index: 10;
}

nav {
  position: absolute; /* Permite usar top y left */
  top: 44px; /* Espacio desde la parte superior */
  left: 173px; /* Espacio desde la parte izquierda */
}

.menu-titles {
  list-style: none; /* Elimina los puntos de la lista */
  margin: 0;
  padding: 0;
  display: flex; /* Alinea los elementos del menú en fila */
  gap: 20px; /* Espacio entre los elementos */
}

.menu-titles li a {
  text-decoration: none; /* Elimina el subrayado de los enlaces */
  text-decoration: none;
  color: white;
  font-size: 16px; /* Color del texto */
}

nav ul {
  display: flex;
  list-style: none;
}

nav ul li {
  margin: 0 10px;
}


.language-switch {
  display: flex;
}

.language-switch a {
  margin-left: 10px;
  text-decoration: none;
  color: white;
  font-size: 13px;
}

.products {
  background-color: #000;
  color: white;
}

.img-full {
  height: 400px;
  background: url('background.jpg') center/cover no-repeat;
}

.box {
  border: 1px solid #ccc;
  text-align: center;
}
    </style>
  </head>
  <body>
    <!-- Header: Menú de Navegación -->
    <header>
      <div class="logo">
        <img src="{{ 'fill-2.png' | asset_url }}" class="logo-tennis" alt="Logo Footer">
      </div>
      <nav>
        <ul class="menu-titles">
          <li><a href="#">Inicio</a></li>
          <li><a href="#">Acerca de Nosotros</a></li>
          <li><a href="#">Historia</a></li>
          <li><a href="#">Contacto</a></li>
        </ul>
      </nav>
      <div class="language-switch">
        <a href="#">ES</a>
        <a href="#">EN</a>
      </div>
    </header>
  
    <div class="container hero">
      <div class="hero-content">
        <h1>Las mejores raquetas para jugar <strong class="strong-text">Tennis</strong></h1>
        <button class="hero-button">ACERCA DE NOSOTROS</button>
      </div>
    </div>
  
    <!-- Sección 2: Raquetas y Texto -->
<div class="container raquetas-section">
  <div class="layout">
    <!-- Columna Izquierda: Texto "Tennis" -->
    <div class="column third text-left">
      <h2 class="title-left">TENNIS</h2>
    </div>

    <!-- Columna Central: Título y descripción -->
    <div class="column third text-center">
      <h2 class="title-center">
        Raquetas. Accesorios.<br>Pelotas.
      </h2>
      <p class="paragraph-about">Conoce nuestras raquetas y aprende a jugar Tennis como un profesional.</p>
    </div>

    <!-- Columna Derecha: Botón -->
    <div class="column third text-right">
      <button class="history-button">Historia</button>
    </div>
  </div>
</div>
  
    <!-- Sección 3: Carrusel de Productos -->
<div class="container products">
  <div class="layout">
    <!-- Imagen con texto superpuesto -->
    <div class="column image-column">
      <div class="text-overlay">
        <h2>Nuestras <strong>Raquetas</strong></h2>
        <p>Conoce nuestras raquetas y aprende a jugar Tennis como un profesional.</p>
        <!-- Indicadores (Dots) y Flechas debajo del texto -->
        <div class="carousel-controls">
          <div class="carousel-indicators">
            <span class="dot active"></span>
            <span class="dot"></span>
            <span class="dot"></span>
          </div>
          <div class="arrows">
            <button class="arrow-left">&#9665;</button>
            <button class="arrow-right">&#9655;</button>
          </div>
        </div>
      </div>
    </div>

    <!-- Carrusel de productos -->
    <div class="column carousel-column">
      <div class="carousel">
        <div class="carousel-inner">
          <!-- Card 1 -->
          <div class="carousel-item">
              <div class="circle">1</div>
              <img src="{{ 'group-10.png' | asset_url }}" class="img-carousel" alt="1">
          </div>
          <!-- Card 2 -->
          <div class="carousel-item">
              <div class="circle">2</div>
              <img src="{{ 'group-10.png' | asset_url }}" class="img-carousel" alt="2">
          </div>
          <!-- Card 3 -->
          <div class="carousel-item">
              <div class="circle">3</div>
              <img src="{{ 'group-10.png' | asset_url }}" class="img-carousel" alt="3">
          </div>
          <!-- Card 4 -->
          <div class="carousel-item">
              <div class="circle">4</div>
              <img src="{{ 'group-10.png' | asset_url }}" class="img-carousel" alt="4">
          </div>
        </div>
      </div>
    </div>
  </div>
</div>




  
    <!-- Sección 4: Imagen Completa -->
    <div class="container">
      <div class="layout">
        <div class="column full img-full">
          <!-- Imagen Completa -->
        </div>
      </div>
    </div>
  
    <!-- Sección 5: Footer -->
    <div class="container footer">
      <div class="layout">
        <div class="column half">
          <p>Menú | Redes Sociales</p>
        </div>
        <div class="column half">
          <p>Logo Footer</p>
        </div>
      </div>
    </div>
  </body>
  </html>

<script>
// Carrusel funcional
const carousel = document.querySelector('.carousel');
const leftArrow = document.querySelector('.arrow-left');
const rightArrow = document.querySelector('.arrow-right');
const dots = document.querySelectorAll('.dot');

let currentIndex = 0;

// Función para mover el carrusel
function moveCarousel(index) {
  const carouselInner = document.querySelector('.carousel-inner');
  const itemWidth = document.querySelector('.carousel-item').offsetWidth;
  carouselInner.style.transform = `translateX(${-itemWidth * index}px)`;

  // Actualizar indicadores (dots)
  dots.forEach((dot, i) => {
    dot.classList.toggle('active', i === index);
  });
}

// Evento de flechas
rightArrow.addEventListener('click', () => {
  currentIndex = (currentIndex + 1) % dots.length;
  moveCarousel(currentIndex);
});

leftArrow.addEventListener('click', () => {
  currentIndex = (currentIndex - 1 + dots.length) % dots.length;
  moveCarousel(currentIndex);
});

// Evento de dots
dots.forEach((dot, i) => {
  dot.addEventListener('click', () => {
    currentIndex = i;
    moveCarousel(currentIndex);
  });
});

</script>

  